name: One-off audit repair 515880 Trial sync
on:
  push:
    paths:
      - ".github/workflows/oneoff-audit-repair-515880-trial-sync.yml"
permissions:
  contents: write
jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Finalize transaction-index synchronization
        run: python scripts/oneoff_finalize_trade_sync.py
      - name: Run focused pre-commit checks
        run: |
          python -m py_compile scripts/process_state_sync_request.py
          python scripts/check_trade_event_formal_sync.py
      - name: Commit final production repair
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ETF交易复盘与经验库_2026.md scripts/process_state_sync_request.py
          git commit -m "fix: keep human trade index synchronized with fills"
          git push origin HEAD:main
