name: ETF share-flow Stage1 PoC

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "requests/research_backfill/etf_share_flow_run_trigger.txt"

permissions:
  contents: write

concurrency:
  group: etf-share-flow-stage1
  cancel-in-progress: false

jobs:
  stage1:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      ETF_SYSTEM_ROOT: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install bounded dependencies
        run: pip install numpy==1.26.4 pandas==2.2.2 scipy==1.13.1 requests==2.32.3
      - name: Run official-share Stage1 PIT validation
        run: python scripts/run_etf_share_flow_stage1.py requests/research_backfill/20260825_etf_share_flow_stage1.json
      - name: Enforce research-only boundary
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          p=json.loads(Path('research/backtests/etf_share_flow_stage1_validation.json').read_text(encoding='utf-8'))
          s=json.loads(Path('data/state/etf_share_flow_research_status.json').read_text(encoding='utf-8'))
          assert p['decision_eligible'] is False
          assert p['trade_signal'] is None
          assert p['trial_confirm'] is None
          assert p['portfolio_target'] is None
          assert p['master_override'] is False
          assert p['historical_decision_prohibited'] is True
          assert s['decision_eligible'] is False
          print(json.dumps({'interpretation':p['research_interpretation'],'passing':p['passing_signals'],'coverage':p['coverage']},ensure_ascii=False))
          PY
      - name: Commit compact research result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add research/backtests/etf_share_flow_stage1_validation.json data/state/etf_share_flow_research_status.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "research: record ETF share-flow stage1 validation"
          git pull --rebase origin main
          git push origin HEAD:main
