# ETF与市场监测数据接口使用规范

> 版本：V1.14  
> 更新日期：2026-08-28  
> 定位：本文是ETF系统**数据与市场监测域的唯一规范性规则来源（Single Source of Truth）**。负责三层监测、数据可用性、质量验收、跨市场时点、market phase、provider降级、代理边界、脉冲与查询时新鲜度等稳定规则语义；不是交易规则来源，不产生买卖动作。  
> 规则边界：交易权限、风险许可、Trial／Confirm、金额与卖出规则只以 `ETF规则_MASTER.md` 为准。机器配置、workflow、脚本、状态文件和测试只负责实现、运行、记录或验证本文规则，不得独立产生第二套数据资格、时点、降级、代理或监测语义。

**规则语义与动态运行事实严格分离。** 本文回答“数据应该怎样取得、验证、解释和降级”；当前对象级provider顺序、ETF运行全集、具体runtime参数、交易日历和当前状态分别以 `config/market/provider_priority.json`、`config/market/etf_monitor_universe.json`、`config/runtime_policy.json`、正式交易日历及 `data/state/` 中的最新有效机器事实为准。文中出现的当前数量、provider链或参数值只用于人类可读审计，并受一致性检查约束；若与机器权威冲突，应修复文档/实现漂移，不得让Markdown反向覆盖当前机器事实，也不得让机器实现反向创造新的数据规则。

## 1. 规范职责与数据链

机器行情必须经过：

```text
数据获取 → 数据质量检查 → 时点与市场阶段对齐 → 行情事实 → 机会判断 → 交易决策
```

本规范只覆盖前三步及“行情事实能否被使用”的边界。接口返回成功不等于数据可用；数据可用不等于形成机会；行情事实不得绕过MASTER直接生成交易动作。

数据优先级与职责保持：MASTER > Dashboard > 当前行情与实际成交 > 经验库 > 行情档案 > 历史聊天。券商账户、真实持仓、现金和实际成交不得由行情接口推定或覆盖。

数据是正式分析的前置基础。不存在足够新鲜、时点清晰且通过质量检查的数据时，只能明确说明数据不足；不得用旧值、不同市场错位时点或未核验接口结果补齐结论。

### 1.1 用户可见语言门禁（强制）

**机器语言与用户语言必须分层。** 状态码、内部字段名、英文枚举、运行对象名和数据结构术语可以保留在代码、JSON、workflow、日志和技术审计中，但凡面向用户的正式行情、盘前、盘中、午间、盘后、交易判断和普通行情答复，必须先转换成自然、明确的中文业务语言；不得把内部实现语言直接作为结论展示，也不得要求用户自行理解机器字段。

基础状态统一翻译：`PASS`→“数据可用”，`DEGRADED`→“数据受限”，`FAILED`→“当前不可用”；`FRESH`→“时点正常”，`DELAYED`→“存在延迟”，`STALE`→“时点过旧”，`SESSION_REFERENCE`→“已结束交易时段参考”，`PREVIOUS_SESSION_REFERENCE`→“上一交易时段参考”。美股阶段统一写成：`PRE_MARKET`→“美股盘前”，`REGULAR`→“美股常规交易时段”，`POST_MARKET`→“美股盘后”；正常回复不得写成“当前阶段：PRE_MARKET”之类机器枚举。

正式交易与账户表达也必须完成业务翻译。`Known-net`不得直接作为用户可见口径名，统一按上下文写成“已确认费用口径下的ETF策略权益”“已确认费用口径下的ETF累计损益”或“按当前已确认费用计算的ETF策略风险率”；`canonical CURRENT`统一表达为“当前正式行情”或“本次正式行情快照”；`decision_context`统一表达为“当前决策证据”或“系统已汇总的决策证据”；`market phase`统一表达为“市场阶段”；`provider`在无需技术区分时统一表达为“数据源”。用户可见正式回复不得直接堆叠内部字段名来替代业务结论。

输出顺序遵循“**先业务结论，后必要口径说明**”。如果一个内部术语对理解结论没有增量价值，应完全隐藏；如果确有必要保留技术精度，应先给中文业务含义，再在括号中附机器名。只有用户明确要求技术审计、系统维护、代码诊断、provider核验或机器状态时，才允许直接展示内部枚举、字段名和文件名。

以下内容不属于应被消除的内部语言：MASTER正式定义且用户需要据此执行的 `Trial／Confirm`，证券名称与证券代码，以及在数据源比较、故障降级或审计中确有辨识价值的正式provider名称。保留这些术语时仍应让句子本身以中文业务表达为主，不能退回机器日志式输出。

### 1.2 停牌／暂停交易与行情失败的分离

证券被provider明确标记为停牌或暂停交易时，属于有效的市场状态事实，不得误记为行情provider失败。系统应保留真实provider时点、最后有效参考价／昨收及明确的`trading_status`、`tradable=false`；不存在实际成交时不得伪造开高低、成交量或可成交价格。该对象可以在“市场状态数据可用”的前提下明确标注当前不可交易，并继续参加持仓事实、风险暴露和后续复牌监测；只有provider本身无法取得或无法确认对象状态时，才按数据受限／当前不可用处理。正常连续竞价对象仍须遵守完整行情字段和时点质量门禁，不因本条降低标准。

## 2. 三层市场监测结构

### 2.1 第一层：指数

正式固定监测：上证指数（000001.SH）、创业板指（399006.SZ）、科创50指数（000688.SH）、纳斯达克100指数（NDX）、费城半导体指数（SOX）、日经225指数（N225）、韩国综合指数（KOSPI）、台湾加权指数（TWII）、恒生科技指数（HSTECH）。

指数层属于市场环境与传导层，不是交易池。任何单一指数不得直接生成ETF动作。单一对象暂时获取失败时，正式用户输出必须写成“数据受限”／“当前不可用”或说明所用代理，不得直接向用户暴露机器状态码，也不得静默从正式监测中消失。机器JSON内部可保留英文枚举供程序判断。

### 2.2 第二层：ETF

ETF层只使用“持仓ETF + 观察ETF”两种身份。两类均持续获取行情、参加机会扫描和统一资本比较；持仓／观察身份由当日账户事实和Dashboard维护，不在接口脚本中写死。

机器采集对象唯一运行清单：`config/market/etf_monitor_universe.json`。生产采集脚本必须读取该清单，不得维护第二份硬编码ETF列表。

当前人类可读快照为11只：561980、588000、159781、159941、513180、159561、513520、518880、159992、515880、159326。**该列举不具有独立运行权威，当前对象与数量始终以 `config/market/etf_monitor_universe.json` 为准。** 名单发生新增或删除时，应由一致性检查保证Dashboard、运行清单与本文审计说明不存在漂移，而不是要求高频生产链等待文档更新后才能采集。

### 2.3 第三层：个股

默认个股监测不设固定股票名单。

- 当前打新底仓：从当日 `data/state/account_fact.json` 的实际非ETF持仓动态识别，并结合 `data/state/asset_roles.json` 判断资产角色；确认 `IPO_BASE_STOCK` 后进入默认监测与统一资本比较。
- 新出现但角色未知的个股：标记 `UNCLASSIFIED_STOCK`，要求一次事实确认，不依据代码、名称或历史持仓自行归类。
- 数量归零：自动退出当前默认个股监测。
- 条件产业链个股：仅在ETF候选、持仓、行业冲击或产业链传导分析需要时动态调用，可覆盖A股、美股、港股、韩股、台股、日股及其他已核验市场，不维护永久名单。

产业链个股只提供背景、传导、增强或反向证据，不作为ETF领先信号，也不单独生成ETF动作。

## 3. 数据源体系与优先级

系统采用多源体系，不把“数据接口”理解为仅同花顺API。**对象级当前主备顺序只以 `config/market/provider_priority.json` 为唯一机器权威；本节的具体链路描述是受一致性检查约束的人类可读审计快照，不构成第二套provider优先级。**

**腾讯行情（tencent_qq）**：当前A股实时生产主源。已验证覆盖上证指数（000001）、创业板指（399006）、科创50指数（000688）、全部当前ETF监测对象、账户A股个股及查询时动态A股产业链个股。腾讯返回成功仍须经过字段、真实provider时间戳、市场阶段和质量门禁；A股对象采用腾讯不代表腾讯可自动扩展为海外生产源。

**hithink-finance（同花顺金融数据CLI/API）**：当前主要承担A股对象的已验证直连第一备源、ETF历史日线，以及部分已审计直接指数。腾讯主源失败时按对象级策略切换同花顺；不得因同花顺仍可用而把旧的“同花顺主源”表述继续写入正式文件。

**Yahoo Chart API**：当前仍是纳斯达克100指数（NDX）、费城半导体指数（SOX）等正式海外指数的主要直接源，也用于海外产业链个股、商品/宏观辅助对象和美股扩展时段5分钟级行情。经2026-08-27真实开盘验证，日经225指数（N225）、韩国综合指数（KOSPI）、台湾加权指数（TWII）的Yahoo盘中路径存在明显延迟或完整分钟OHLC不足，因此对这三个对象降为交叉验证/后备路径。Yahoo返回成功仍须经过时点、市场阶段和质量检查。恒生科技指数的Yahoo正式映射为 `HSTECH.HK`，不得使用已验证返回404的 `^HSTECH`。

**Eastmoney push2（东方财富公开行情接口）**：当前为恒生科技指数（HSTECH）的正式主源，使用已在真实交易时段验证的 `124.HSTECH`；同时作为除上证指数（000001）外多数已验证A股ETF/个股/指数对象的后备直连源。上证指数的 `secid=1.000001` 已验证并非可靠指数映射，因此不得作为上证指数备源。所有东方财富行情必须读取并校验provider行情时间戳，不得用workflow执行时间冒充行情时间。

**TWSE MIS（台湾证券交易所官方即时行情）**：2026-08-27台湾市场开盘三轮并行实测中，台湾加权指数（TWII）的 `tse_t00.tw` 均返回HTTP 200、真实 `tlong` provider时间戳及完整OHLC/昨收，时点约落后19—22秒，优于本轮东方财富约29—51秒及Yahoo分钟数据缺失，现为台湾加权指数（TWII）正式主源。当前仅验证TWII，不自动扩展至其他台股对象。

当前A股对象级生产链以 `config/market/provider_priority.json` 为唯一机器权威：上证指数（000001）为 `腾讯 → 同花顺`；科创50指数（000688）为 `腾讯 → 同花顺`；创业板指（399006）、当前11只ETF和已确认账户A股个股原则上为 `腾讯 → 同花顺 → 东方财富`；查询时动态A股产业链个股为 `腾讯 → 同花顺`。对象映射未经逐项验证不得自行加入fallback。

恒生科技指数（HSTECH）的当前正式链为：`eastmoney_push2:124.HSTECH → yahoo_chart_api:HSTECH.HK → hithink-finance:HS2083 → ETF_PROXY_513180`。前三项均为直接指数路径；只有直接路径均不可用时，才允许使用恒生科技ETF（513180）作为 `PROXY` 辅助，代理不得冒充恒生科技指数本身。

日经225指数（N225）的当前正式链为：`eastmoney_push2delay:100.N225 → eastmoney_push2:100.N225 → yahoo_chart_api → ETF_PROXY_513520`。2026-08-27真实开盘多轮测试中，东方财富低延迟endpoint保持秒级，而Yahoo约延迟15分钟。进一步测试Stooq、日经官方页面、Naver `NII@NI225`、Google Finance后，仍未获得另一个同时满足独立provider、秒级/近实时、真实可审计时间戳与稳定结构化OHLC的正式候选；Naver页面明确为15分钟延迟。因此N225当前仍存在“缺少第二个独立秒级实时直连源”的冗余不足，必须如实保留，不得把东方财富两个host计作两个独立源。

韩国综合指数（KOSPI）的当前正式链为：`naver_finance:KOSPI → eastmoney_push2delay:100.KS11 → eastmoney_push2:100.KS11 → yahoo_chart_api`。Naver使用`localTradedAt`和市场状态，作为经真实开盘并行验证的独立实时主源。

台湾加权指数（TWII）的当前正式链为：`twse_mis:tse_t00.tw → eastmoney_push2delay:100.TWII → eastmoney_push2:100.TWII → yahoo_chart_api`。TWSE使用`tlong`作为provider时点；东方财富两个host属于同一provider家族；Yahoo保留为延迟型独立交叉验证/最后后备。

其他直接指数不可用时，也可使用已定义ETF代理辅助，但必须明确标注 `PROXY`。例如：SOXQ仅可作为SOX备用代理；513520可辅助N225；518880可辅助黄金风险路径。代理是降级工具，不得永久替代已经可稳定获取的直接指数。

美股扩展时段与现金指数必须分开：NDX/SOX用于正式现金盘结构；QQQ/SOXX可作为纳指100与半导体方向的扩展时段代理。美股产业链个股如INTC、NVDA、AMD等只在当前假设需要时动态调用，不形成永久固定名单。

AKShare、pandas-datareader等可作为候选二级适配器，只有完成真实环境验证、字段映射、时区验证和质量审计后才能升级为正式生产源。未经验证的数据源不得因为“能返回数据”进入正式决策链。

数据源对象级优先级以 `config/market/provider_priority.json` 为机器配置唯一来源；三层监测对象以 `config/market/market_monitor_config.json`、`config/market/etf_monitor_universe.json` 和动态账户事实共同确定。

## 4. 盘前与盘中脉冲原则

云端采集采用“事件节点 + 目标频率 + 新鲜度判断”，不是固定截图，也不是假设GitHub Actions绝对准点。

A股生产脉冲必须先通过交易日与交易时段门禁。`config/market/a_share_trading_calendar_2026.json` 记录交易所官方休市安排；计划任务必须同时满足：日期在日历覆盖范围内、不是周末、不是交易所官方休市日，并处于允许采集窗口或收盘宽限窗口。不能仅凭“周一到周五”推定交易日。

### 4.1 盘前启动顺序

市场监测不得等到A股9:30连续竞价才启动。

- 美股上一现金盘：NDX/SOX作为前序外部结构读取；
- 美股盘后：云端从北京时间07:00开始刷新QQQ/SOXX扩展时段状态，捕捉上一美股交易日盘后的最后阶段；美国夏令时盘后通常约北京时间08:00结束，冬令时通常约09:00结束，由 `America/New_York` 时区自动换算，不硬编码固定北京时间终点；
- 美股下一交易日盘前：Nasdaq盘前为美东04:00—09:30，通常对应北京时间16:00—21:30（夏令时）或17:00—22:30（冬令时）。因此它通常发生在A股收盘后，是**下一A股交易日**可能使用的前置信号，不得误称为当天A股上午的同步领先行情；
- 日经225与KOSPI：北京时间08:00进入常规交易，海外/亚洲指数预开盘脉冲持续刷新；
- 台湾加权：北京时间09:00进入常规交易；
- 恒生科技：北京时间09:00进入港股盘前阶段，09:30进入常规交易；
- A股ETF与指数：北京时间09:15进入开盘集合竞价，09:15、09:20、09:25为集合竞价专用脉冲，09:30起转为连续竞价10分钟级脉冲。

盘前海外脉冲由 `.github/workflows/overseas-preopen-pulse.yml` 负责，生成 `overseas_context.json` 与 `us_extended_hours_context.json`；A股集合竞价及盘中脉冲由 `.github/workflows/market-snapshot.yml` 负责。

### 4.2 各市场正式交易时点（固定口径）

正式输出和跨市场对齐统一以下表为人类可读时间口径；机器判断仍以交易日历、`market_timezone`、`market_phase`和provider实际时间戳为准。遇交易所临时休市、半日市或规则调整，以当日交易所日历为更高优先级，不用本表硬覆盖。

|市场|市场本地正式时段|北京时间常规对应|说明|
|-|-|-|-|
|A股|集合竞价09:15–09:25；连续竞价09:30–11:30、13:00–15:00|同左|09:25–09:30为开盘衔接阶段，不按连续成交解释|
|港股|开市前09:00–09:30；持续交易09:30–12:00、13:00–16:00|同左|收盘后使用当日正式收盘；HSTECH按实际provider阶段解释|
|台股|常规09:00–13:30|同左|无A股式午间分段|
|日股|常规09:00–11:30、12:30–15:30（东京时间）|08:00–10:30、11:30–14:30|东京时间比北京时间快1小时|
|韩股|常规09:00–15:30（首尔时间）|08:00–14:30|首尔时间比北京时间快1小时|
|美股|盘前04:00–09:30、现金盘09:30–16:00、盘后16:00–20:00（美东时间）|夏令时：盘前16:00–21:30、现金盘21:30–次日04:00、盘后次日04:00–08:00；冬令时整体顺延1小时|美东本地时间为权威；北京时间跨自然日时必须标明对应美国交易日|

不同市场比较时必须显示各自【数据时点（北京时间）】，不得为了表格整齐把非同步市场强行改写成同一时间。

### 4.3 美股扩展时段语义

美股信息必须区分：`REGULAR`（正式现金盘）、`POST_MARKET`（盘后）、`PRE_MARKET`（盘前）。禁止把三者合并成“美股当前行情”。

Nasdaq常规盘为美东09:30—16:00，盘前为04:00—09:30，盘后为16:00—20:00。扩展时段流动性通常较低、价差更大、波动更高，因此扩展时段涨跌只能作为前置信号。

例如“英特尔盘前上涨5%”可以成为半导体链的增强证据，但必须同时标注：标的、`PRE_MARKET`、北京时间as_of、相对上一正式现金盘收盘的涨跌，以及数据来源。它不能等同于SOX正式指数上涨5%，也不能绕过“外部结构 → 本地传导 → ETF自身反馈 → 机会判断”。

原则是：**不高估海外时间领先，也不低估海外时间领先。** 已经完成的美国盘后/盘前价格发现必须读取；尚未发生的美国盘前不能虚构为领先信息；扩展时段存在真实价格信号，但证据权重低于正式现金盘且需本地验证。

### 4.4 A股集合竞价语义

09:15—09:25属于 `OPENING_CALL_AUCTION`。该阶段交易所即时行情与连续竞价字段语义不同；即使provider返回open/high/low/last等通用字段，也只能作为集合竞价时点快照解释，不得与09:30后的连续成交最新价、成交量价结构直接混用。正式机会、金额或卖出判断若依赖连续成交承接，应等待连续竞价数据。

### 4.5 目标频率与新鲜度

当前运行参数以 `config/runtime_policy.json` 为唯一机器来源；以下数值是受一致性检查约束的人类可读当前快照，不构成第二套运行参数：

- `target_cadence_seconds = 600`：常规目标10分钟脉冲；
- `fresh_max_age_seconds = 900`：数据年龄不超过15分钟为 `FRESH`；
- `degraded_max_age_seconds = 1500`：15—25分钟为 `DEGRADED`；
- 超过25分钟为 `STALE`；
- `close_grace_seconds = 1800`：15:00收盘任务允许30分钟调度宽限，仅用于收盘后补取当日最终收盘事实，不扩展连续竞价或可成交时段语义；
- `provider_timeout_seconds = 25`：单次接口调用超时25秒；
- `provider_retry_limit = 2`：有限重试，不无限阻塞；
- `scheduled_provider_max_workers = 1`：定时任务单并发；
- `query_provider_max_workers = 2`：查询时补采最多2并发；
- workflow单次运行最长6分钟。

核心原则：

1. **10分钟是常规采集目标，不是决策时钟。** 集合竞价等关键阶段可以使用更密事件脉冲；判断行情可用性仍看实际数据时点。
2. **查询时重新计算数据年龄。** `CURRENT.json` 曾经写过FRESH，不代表稍后查询仍然FRESH。
3. **失败不覆盖上一有效状态。** 本次采集失败时保留上一有效CURRENT与快照，在 `runtime_health.json` 记录FAILED。
4. **旧任务不得覆盖新任务。** 旧运行晚到时必须被取消或标记 `SUPERSEDED`。
5. **收盘节点优先完整。** 15:00任务因调度延迟可在宽限窗口补采；当日close已成功写入后不重复覆盖。
6. **只有新有效快照才允许构建下游上下文。** runner返回成功但实际为SKIPPED、SUPERSEDED、重复close或probe-only时，不得继续生成新的下游context。
7. **脉冲不是逐笔实时行情。** 云端属于近实时状态采集，不得描述为交易所tick级实时流。

数据使用边界：`FRESH`可进入当前行情判断；`DEGRADED`只作背景和连续性复核；`STALE`不得冒充当前行情；`FAILED`不得进入行情事实。

### 4.6 查询时行情获取固定优先级

盘前、集合竞价、盘中、收盘后即时检查以及用户上传券商截图后触发的正式分析，行情获取不得默认先使用最近一次定时快照。固定优先级为：

```text
查询时立即补采 → 最近一次有效快照 → 明确降级／缺失
```

1. **第一顺位：查询时立即补采。** 在当前市场允许查询且正式分析需要当前行情时，优先通过系统已验收的按需／恢复入口获取三层结构当前可取得的新行情。补采必须经过与生产行情相同的数据质量、provider时间戳、市场阶段和时点检查；“请求已发出”不等于“补采成功”。
2. **第二顺位：最近一次有效快照。** 只有即时补采失败、超时、限流、对象暂不可得或查询场景不允许补采时，才回退到最近有效生产快照，并按查询时刻重新计算FRESH／DEGRADED／STALE。最近快照不能因为曾经标记FRESH而永久沿用。
3. **逐对象采用最新有效证据。** 部分对象补采成功、部分失败时，成功对象使用新数据；失败对象才分别回退最近有效快照。不得为了统一时点把已成功补采的对象整体降回旧快照。
4. **账户事实与行情时点分开。** 用户券商截图负责账户、持仓、现金和实际成交事实；收到截图后应优先重新查询行情。截图时间不得冒充ETF、指数或个股行情时间，市场行情必须显示各自实际provider/capture时点。
5. **失败必须可见。** 即时补采和最近快照均不可用时，明确标记DEGRADED／STALE／FAILED或数据缺口，不得用旧行情、不同对象代理或用户截图中的非目标行情无声补齐。
6. **不因补采改变交易规则。** 查询时补采只提高数据时效性，不增加Trial／Confirm权限，不改变风险许可、金额和卖出规则。
7. **同一用户请求只允许一条即时补采链。** 本次请求发起后若尚无合格的新行情，可以触发一次按需补采；不得因为等待、轮询或分析进行中再次创建第二条等价补采链。该链形成第一份满足当前市场阶段、质量和新鲜度要求的可用行情后，即停止等待并进入最小充分判断，不为了获得“更新一点”的第二份行情继续阻塞。
8. **正式回复前只做一次最终行情复核。** 在正式判断已经形成、回复尚未发送时，最后检查一次本次请求对应的新行情是否已经完成；若已有更新且合格，必须纳入本次回复；若没有，则按当前已取得的合格行情继续，不循环等待。补采失败或超时才按既有新鲜度规则降级，并明确真实数据时点。

因此，用户在例如10:58提交券商截图时，正式分析的账户事实时点可以是10:58，但A股ETF／指数应优先使用收到请求后新取得的实际行情时点；只有补采失败时才使用例如10:50附近最近有效快照。海外与亚洲对象仍逐市场显示各自 `as_of_beijing` 和 `market_phase`，不得把分析发起时间统一套用为所有对象的数据时间。

### 4.7 ChatGPT查询来源自主路由（强制）

ETF项目内凡涉及“最新行情、当前价格、今日涨跌、盘前／盘后价格、指数或证券当前状态”等行情事实查询，**用户无需指定“联网”“ETF系统”“腾讯”或具体provider，也无需为了让系统工作而先提供证券代码。ChatGPT必须自行识别对象、市场、查询目的和时效要求，并自主选择最有效的数据获取路径；不得把数据来源选择责任反向交给用户。**

固定职责边界如下：

1. **普通临时公开行情查询：默认优先联网。** 对不要求正式ETF交易决策、账户一致性、生命周期或系统专有证据的公开行情事实，优先使用当前公开Web来源，并自主比较时效、来源可靠性、market phase、可审计性和回答成本；不得因为对象不在ETF正式监测池就要求用户改用另一种提问方式。
2. **正式ETF分析：系统正式事实优先。** 盘前、集合竞价、盘中、午间、盘后、金额、卖出、生命周期、资本比较等正式ETF判断，必须优先消费canonical CURRENT、正式账户事实、decision context及系统允许的其他正式证据；普通Web行情只能作为外部补充，不得冒充canonical CURRENT、账户事实或正式provider输出，不得绕过系统数据降级语义生成交易动作。
3. **系统有明确增量能力时主动调用。** 即使用户没有点名ETF系统，只要腾讯A股quote、腾讯1分钟路径、market structure、相对强弱、研究执行桥、账户事实或其他正式内部证据可能实质改变判断，ChatGPT应主动调用并消费这些证据，而不是机械停留在Web结果。
4. **Web结果冲突或质量不足时主动交叉验证。** 当公开结果存在来源冲突、时点不清、market phase不明、代理与本体混淆或明显陈旧时，应主动使用交易所／可靠财经源交叉核验，必要时再调用ETF系统已验证provider链；不得把“搜到了一个价格”当作质量验收完成。
5. **用户明确指定来源时服从约束。** 用户点名某一来源、腾讯分钟源、ETF系统或要求联网时，除非该来源不可用或会违反正式数据边界，应按其要求执行并明确异常；但用户指定来源只是偏好／约束，不是正常行情查询的必要前提。
6. **场景升级必须自动升级证据。** 一个短问题即使表面像查价，只要其真实目的涉及持仓、观察ETF、风险许可、Trial／Confirm、金额、卖出、资本迁移或跨市场传导，ChatGPT必须自动把任务升级到相应正式读取与数据链，不得因为用户没有写“正式分析”而继续使用普通临时查询标准。
7. **接近执行或出现异常时主动补外部增量证据。** 当唯一主候选已经接近Trial／Confirm、持仓出现仅靠价格结构难以解释的明显异常强弱、产业链／政策／宏观事件可能改变当前假设，或外部公开事实可能实质改变机会、金额、卖出或资本用途时，ChatGPT应主动联网核验最相关的事件催化和反向证据，不要求用户另行指定“查新闻”。联网补充只服务于可能改变正式决议的信息；普通新闻、重复背景、无法形成Point-in-Time证据或不能改变决议的内容不得为了“信息完整”阻塞正式分析。外部证据仍须遵守时点、来源可靠性和“外部结构→本地传导→ETF自身反馈→机会判断”边界，不得绕过MASTER直接生成动作。

该自主路由是**来源选择与证据强度的强制责任**，不是允许ChatGPT绕过数据质量门、MASTER或正式系统事实。目标不是机械“Web优先”或机械“ETF系统优先”，而是由ChatGPT在规则边界内对任务分类并选择能够最大化事实质量、时效性和决策效率的路径；用户不负责替系统完成该分类。

## 5. 强制数据时点与跨市场时间对齐

### 5.1 正式输出强制时间戳

任何正式行情分析、ETF判断、盘中复核或盘后复盘，只要引用行情数据，必须明确标注数据时点。禁止输出“当前、最新、刚刚”而不给可核验时间。

统一展示口径为**北京时间（Asia/Shanghai，UTC+8）**：

```text
【数据时点（北京时间）】2026-08-24 09:20:xx
```

A股ETF、A股指数和账户截图均优先按北京时间标注。海外/亚洲对象必须至少提供 `as_of_beijing`；同时保留 `market_timezone`、`as_of_local`、`market_phase` 和 `time_relation_to_a_share` 用于解释。美股扩展时段对象必须同时显示 `market_phase_of_latest = PRE_MARKET / REGULAR / POST_MARKET`。

若一个正式分析引用多个不同数据时点，不能只给一个笼统总时间，必须对存在明显错位的对象分别标注。若provider返回值相对当前查询时间存在延迟，输出不得隐藏延迟；应直接显示其 `as_of_beijing`，必要时注明“距当前约X分钟”。数据时间本身就是质量信息。

### 5.2 跨市场对齐

跨市场数据必须携带或能够明确推导：`as_of_beijing`、`market_timezone`、`as_of_local`、`market_phase`、`time_relation_to_a_share`。不得按自然日期相同就认定为同步行情。

- 美国现金指数：A股交易时段主要对应上一美股正式现金盘；
- 美国盘后：可能对次日A股开盘形成领先信息，冬夏令时导致与北京时间重叠程度不同；
- 美国下一交易日盘前：通常在A股当天收盘后出现，主要影响下一A股交易日，不能倒置时间关系；
- 日经、韩国、台湾：根据各自市场实际阶段区分同日盘中、同日已收盘或上一交易日；
- 恒生科技：A股15:00收盘时香港市场通常尚未完成当日收盘；
- 不同市场上一收盘、盘后、盘前、当前盘中和当日收盘不得混成一组“共振”证据。

跨市场信息进入交易判断的顺序始终为：

```text
外部结构（先完成时点对齐） → 本地传导 → ETF自身反馈 → 机会判断
```

## 6. 数据质量验收

每个生产对象至少检查：标的身份与代码、允许的数据源、关键字段、OHLC逻辑、数据时点、时区、市场阶段、provider时间戳、交易日/交易时段、累计成交量/额语义、多源冲突、代理与直接指数区分、休市旧数据识别以及账户事实来源。

强制时间字段：A股快照至少保存 `captured_at_beijing` 与 `market_phase`；海外/亚洲指数至少保存 `latest.as_of_beijing`、`market_timezone`、`market_phase_at_generation` 与 `time_relation_to_a_share`；美股扩展时段至少保存 `latest.as_of_beijing`、`market_phase_of_latest`、`regular_session_close_reference` 与 `extended_change_vs_regular_close_pct`。缺少可解释数据时点的对象不得按FRESH进入正式行情判断。

相邻脉冲变化由 `data/state/market_delta.json` 保存，只表达客观价格、高低、成交量／成交额变化，不设置固定阈值、机会等级或交易动作。

## 7. 生产状态与审计文件

核心运行文件包括：`CURRENT.json`、`runtime_health.json`、`market_delta.json`、`overseas_context.json`、`us_extended_hours_context.json`、`account_fact.json`、`stock_context.json`、`stock_market_context.json`、`query_context.json`、`system_consistency.json`。核心运行配置还包括A股官方交易日历、provider优先级、ETF运行全集和runtime policy。

`us_extended_hours_context.json` 默认只保留QQQ/SOXX作为扩展时段方向代理；条件美股产业链个股由 `US_EXTENDED_SYMBOLS` 或查询时外部核验动态加入，不形成永久名单。

生产原始响应、快照与审计材料应保持可追溯。失败记录不得伪装成成功快照，历史回放不得写入生产CURRENT。

## 8. 故障与降级

- GitHub Actions延迟：使用真实数据时点重新判定新鲜度；
- 工作日但交易所官方休市：session gate直接跳过；
- runner正常返回但未写入新有效快照：下游context全部跳过；
- 同花顺、Yahoo或东方财富局部失败：按对象级provider优先级继续直接源降级并保留监测职责；
- 恒生科技指数：HS2083失败时先尝试Yahoo `HSTECH.HK`，再尝试东方财富 `124.HSTECH`；只有三条直接源均失败才可使用恒生科技ETF（513180）代理，并明确 `PROXY`；
- 美股扩展时段无有效bar：明确FAILED/STALE，不用上一正式现金盘冒充盘后或盘前；
- 美股扩展时段个股大幅异动：保留真实价格信号，但标记扩展时段，不自动提升为正式指数确认；
- 数据格式正常但时间戳陈旧：按时点规则降级，不能视为FRESH；
- 已核实账户事实按事件驱动延续；只有确认发生交易、资金划转、公司行为或其他账户变化后才失效并要求刷新，不因自然日期变化机械失效；
- Git推送冲突：先rebase；不得覆盖正式文件；无法自动解决时保留数据并要求人工检查。

### 8.1 历史行情事实恢复

缺失的历史行情只能通过统一的历史事实恢复合同评估后进入现有行情事实链，不得由研究脚本、查询上下文或账户事实直接写入CURRENT。恢复结果必须区分 `RECOVERABLE_EXACT`、`RECOVERABLE_EOD_EQUIVALENT`、`PARTIALLY_RECOVERABLE` 和 `UNRECOVERABLE`，并保留目标交易日、目标节点、有效市场时点、实际历史检索时间、数据源、覆盖范围、质量和来源引用。

完整且具有原始provider观察时点的目标节点事实，才可作为精确节点候选；仅有历史日线收盘价时，只能作为同日收盘等价证据，不能伪造盘中观察时点或自动提升为精确实时事实。较弱历史证据不得覆盖较强的正式快照，历史恢复也不得使CURRENT倒退到更早交易日。恢复失败、覆盖不全、日期/证券映射不明或交叉校验冲突时，必须保留明确缺口并停止提升资格，不得用插值、附近快照或其他对象价格补齐。

## 9. 系统一致性与代码提交完整性

一致性检查是系统基础操作，不是文本校对。每次相关维护必须至少同时检查：

1. **文本与规则口径**：四个正式文件、一级目录数据规范、统一索引无冲突；
2. **监测对象与配置**：指数、ETF、动态个股、provider、交易日历和runtime参数一致；
3. **运行链**：workflow → session gate → provider → snapshot → CURRENT/runtime_health → downstream context → commit，关键步骤存在且门禁有效；
4. **代码与提交完整性**：关键文件必须被Git跟踪；当前运行必须能记录 `HEAD/GITHUB_SHA`、workflow run id；相关更新完成后必须形成真实commit，不允许“本地/工作树已经改、main未提交”却宣称维护完成；
5. **自动验收结果**：`system_consistency.json` 不得存在硬FAIL；相关commit触发的自动一致性workflow应有可追溯结果；
6. **运行状态**：核心状态文件存在、结构合法；真实交易日还需用实际脉冲验证接口和GitHub Actions执行链；
7. **跨市场扩展时段链**：美股现金盘、POST_MARKET、PRE_MARKET必须可区分；QQQ/SOXX扩展时段脚本、状态文件、workflow和query读取入口必须一致，不能只写在文档中；
8. **恒生科技直接源链**：配置和代码必须同时包含 `HS2083 → HSTECH.HK → 124.HSTECH` 的直接指数降级顺序；任何把 `^HSTECH` 恢复为Yahoo生产代码、或在仍有直接源可用时直接跳到513180代理，都属于一致性错误。

`scripts/check_system_consistency.py` 是基础自动验收脚本；`.github/workflows/system-consistency.yml` 负责维护后自动重算并持久化结果；生产行情workflow在采集前再次预检。硬冲突必须判FAIL并阻断生产；账户事实缺失等正常状态只记WARNING。

一致性检查的对象包括但不限于文本、配置、脚本、workflow、Git跟踪状态、当前commit标识、运行状态文件、自动检查状态和真实生产脉冲。不能以“文档相同”替代系统一致性。

GitHub `main` 是云端唯一主版本；本规范位于一级目录：`ETF与市场监测数据接口使用规范.md`。

### 9.1 一致性结果提交审计语义

一致性脚本中的 `checked_commit` 表示本次检查实际读取和验证的代码/配置提交，通常等于运行时的 `HEAD`，并在 GitHub Actions 中与 `GITHUB_SHA` 对照。后续用于保存 `system_consistency.json` 的状态提交属于 `persisted_state_commit`，它是持久化检查结果的后续提交，不要求、也不应通过递归提交让报告反向引用自身最终SHA。因而，`system_consistency.json` 的 PASS 首先表示 `checked_commit` 通过；审计时应同时查看报告中的持久化提交语义，而不是误读为报告文件必须与最终状态提交使用同一个SHA。

## 10. 边界

本规范不修改20万元ETF计划本金、风险许可阈值、Trial／Confirm生命周期、固定金额档或卖出规则；不自动交易、不自动修改MASTER。

目标：**确保进入交易系统的行情事实完整、及时、可追溯、跨市场时点正确，并让任何延迟、错位、接口失败或代码/运行链不一致都能被直接看见。**

### 东方财富A股ETF直接行情字段归一化

在切换前影子审计及对象级直接fallback中，东方财富 push2 ETF字段必须按以下口径进入统一行情模型：

- `f57`必须与目标证券代码一致，`f58`必须存在并作为证券名称核验字段；上海ETF使用`secid=1.xxxxxx)，深圳ETF使用`secid=0.xxxxxx`。
- `volume_raw=f47`，原始单位为手（`hand`）；统一机器字段`volume=f47*100`，单位为股（`share`）。不得直接把`f47`写入统一成交量字段。
- `amount_raw=f48`，统一`amount=f48`，单位为人民币元（`CNY`）。
- `provider_timestamp=f86`，由其解析`as_of_beijing`；`f124`仅保留作审计参考，不作为默认时间字段。
- 以上字段仍须通过当日日期、freshness、OHLC关系、非负成交量/成交额和市场阶段检查；不得使用旧行情、代理ETF、指数推算或账户价格补齐。
