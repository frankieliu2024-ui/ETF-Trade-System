# ETF与市场监测数据接口使用规范

> 版本：V1.1  
> 更新日期：2026-08-24  
> 定位：ETF云端系统一级目录基础数据规范。负责数据源、三层监测、盘中脉冲、质量验收、跨市场时点、故障降级和维护一致性；不是交易规则来源，不产生买卖动作。  
> 规则边界：交易权限、风险许可、Trial／Confirm、金额与卖出规则只以 `ETF规则_MASTER.md` 为准。

## 1. 规范职责与数据链

机器行情必须经过：

```text
数据获取 → 数据质量检查 → 时点与市场阶段对齐 → 行情事实 → 机会判断 → 交易决策
```

本规范只覆盖前三步及“行情事实能否被使用”的边界。接口返回成功不等于数据可用；数据可用不等于形成机会；行情事实不得绕过MASTER直接生成交易动作。

数据优先级与职责保持：MASTER > Dashboard > 当前行情与实际成交 > 经验库 > 行情档案 > 历史聊天。券商账户、真实持仓、现金和实际成交不得由行情接口推定或覆盖。

## 2. 三层市场监测结构

### 2.1 第一层：指数

正式固定监测：

- 上证指数（000001.SH）
- 创业板指（399006.SZ）
- 纳斯达克100指数（NDX）
- 费城半导体指数（SOX）
- 日经225指数（N225）
- 韩国综合指数（KOSPI）
- 台湾加权指数（TWII）
- 恒生科技指数（HSTECH）

指数层属于市场环境与传导层，不是交易池。任何单一指数不得直接生成ETF动作。单一对象暂时获取失败时必须明确 `DEGRADED`／`FAILED` 或使用明确代理，不得静默从正式监测中消失。

### 2.2 第二层：ETF

ETF层只使用“持仓ETF + 观察ETF”两种身份。两类均持续获取行情、参加机会扫描和统一资本比较；持仓／观察身份由当日账户事实和Dashboard维护，不在接口脚本中写死。

机器采集对象唯一运行清单：`config/market/etf_monitor_universe.json`。生产采集脚本必须读取该清单，不得维护第二份硬编码ETF列表。

当前清单共11只：561980、588000、159781、159941、513180、159561、513520、518880、159992、515880、159326。名单未来发生新增或删除时，必须在同一维护批次同步Dashboard、运行清单、本规范相关说明并执行一致性检查。

### 2.3 第三层：个股

默认个股监测不设固定股票名单。

- 当前打新底仓：从当日 `data/state/account_fact.json` 的实际非ETF持仓动态识别，并结合 `data/state/asset_roles.json` 判断资产角色；确认 `IPO_BASE_STOCK` 后进入默认监测与统一资本比较。
- 新出现但角色未知的个股：标记 `UNCLASSIFIED_STOCK`，要求一次事实确认，不依据代码、名称或历史持仓自行归类。
- 数量归零：自动退出当前默认个股监测。
- 条件产业链个股：仅在ETF候选、持仓、行业冲击或产业链传导分析需要时动态调用，可覆盖A股、美股、港股、韩股、台股、日股及其他已核验市场，不维护永久名单。

产业链个股只提供背景、传导、增强或反向证据，不作为ETF领先信号，也不单独生成ETF动作。

## 3. 数据源体系与优先级

系统采用多源体系，不把“数据接口”理解为仅同花顺API。

### 3.1 已纳入生产／正式使用的数据源

**hithink-finance（同花顺金融数据CLI/API）**

主要职责：A股ETF、A股指数、当前打新底仓A股行情，以及已验证可覆盖的直接指数。A股ETF和上证／创业板指数为主要生产源；HSTECH可优先尝试已验证的HS2083入口。

**Yahoo Chart API**

主要职责：海外及亚洲正式指数、海外产业链个股、商品或宏观辅助对象。当前正式海外指数层主要覆盖NDX、SOX、N225、KOSPI、TWII，并作为HSTECH等对象的允许备用来源之一。Yahoo返回成功仍须经过时点、市场阶段和质量检查，不因来源公开而自动视为实时。

### 3.2 明确代理

直接指数不可用时，可使用已定义ETF代理辅助，但必须明确标注 `PROXY`，不得把ETF代理写成指数本身。例如：SOXQ仅可作为SOX备用代理；513520可辅助N225；513180可辅助HSTECH；518880可辅助黄金风险路径。

代理是降级工具，不得永久替代已经可稳定获取的直接指数。

### 3.3 候选备用数据源

AKShare、pandas-datareader等可作为候选二级适配器，只有完成真实环境验证、字段映射、时区验证和质量审计后才能升级为正式生产源。未经验证的数据源不得因为“能返回数据”进入正式决策链。

数据源对象级优先级以 `config/market/provider_priority.json` 为机器配置唯一来源；三层监测对象以 `config/market/market_monitor_config.json`、`config/market/etf_monitor_universe.json` 和动态账户事实共同确定。

## 4. 盘中脉冲原则

盘中采集采用“目标频率 + 新鲜度判断”，而不是固定截图节点，也不是假设GitHub Actions绝对准点。

当前运行参数以 `config/runtime_policy.json` 为唯一机器来源：

- `target_cadence_seconds = 600`：目标10分钟脉冲；
- `fresh_max_age_seconds = 900`：数据年龄不超过15分钟为 `FRESH`；
- `degraded_max_age_seconds = 1500`：15—25分钟为 `DEGRADED`；
- 超过25分钟为 `STALE`；
- `close_grace_seconds = 900`：15:00收盘任务允许15分钟调度宽限；
- `provider_timeout_seconds = 25`：单次接口调用超时25秒；
- `provider_retry_limit = 2`：有限重试，不无限阻塞；
- `provider_max_workers = 3`：受控并发，避免串行接口耗时吞噬整个脉冲周期；
- workflow单次运行最长6分钟。

核心原则：

1. **10分钟是采集目标，不是决策时钟。** GitHub Actions可能延迟，判断行情可用性必须看实际 `captured_at`，不能看cron计划时间。
2. **查询时重新计算数据年龄。** `CURRENT.json` 曾经写过FRESH，不代表稍后查询仍然FRESH。
3. **失败不覆盖上一有效状态。** 本次采集失败时保留上一有效CURRENT与快照，在 `runtime_health.json` 记录FAILED；下一有效脉冲自动恢复。
4. **旧任务不得覆盖新任务。** 旧运行晚到时必须被取消或标记 `SUPERSEDED`。
5. **收盘节点优先完整。** 15:00任务因调度延迟可在宽限窗口补采；当日close已成功写入后不重复覆盖。
6. **脉冲不是逐笔实时行情。** 云端当前属于10分钟级近实时状态采集，正式分析不得把它描述为交易所tick级实时流。

数据使用边界：

- `FRESH`：可进入当前行情判断；
- `DEGRADED`：只作背景和连续性复核；若会改变机会、金额或卖出动作，应优先等待下一有效脉冲或结合用户当前截图；
- `STALE`：不得冒充当前行情，只能作为历史／背景事实；
- `FAILED`：该次失败数据不得进入行情事实。

## 5. 跨市场时间与交易时段对齐

跨市场数据必须携带或能够明确推导：

- `market_timezone`
- `as_of`／`as_of_local`
- `market_phase`（盘前／盘中／休市／已收盘）
- `time_relation_to_a_share`

不得按自然日期相同就认定为同步行情。

- 美国现金指数和美股个股：A股交易时段通常对应上一美股交易时段的已完成信息，只能作为前序外部结构，不得表述为与A股当前盘中“同步上涨／下跌”。
- 日经、韩国、台湾：根据各自市场实际阶段区分同日盘中、同日已收盘或上一交易日。
- 恒生科技：A股15:00收盘时香港市场通常尚未完成当日收盘，必须按实际 `market_phase` 解释。
- 不同市场上一收盘、当前盘中和当日收盘不得混成一组“共振”证据。

跨市场信息进入交易判断的顺序始终为：

```text
外部结构（先完成时点对齐） → 本地传导 → ETF自身反馈 → 机会判断
```

## 6. 数据质量验收

每个生产对象至少检查：

1. 标的身份与代码是否匹配；
2. 来源是否为允许的数据源，远程响应是否真实成功；
3. 关键字段是否齐全；
4. OHLC逻辑是否成立；
5. 时间戳、时区和市场阶段是否可解释；
6. 当日累计成交量／成交额等字段不能跨日错误求差；
7. 同一对象多源冲突时并列保留来源并降级，不静默覆盖；
8. ETF代理和直接指数必须区分；
9. 休市或接口返回旧数据时不能仅因格式正确判为实时；
10. 当前账户事实必须由当日券商事实确认，行情接口不能推断持仓没有变化。

相邻脉冲变化由 `data/state/market_delta.json` 保存，只表达客观价格、高低、成交量／成交额变化，不设置固定阈值、机会等级或交易动作。

## 7. 生产状态与审计文件

核心运行文件：

- `data/state/CURRENT.json`：最新有效A股/ETF脉冲入口；
- `data/state/runtime_health.json`：调度、采集、失败、耗时与运行健康；
- `data/state/market_delta.json`：相邻有效脉冲的客观变化；
- `data/state/overseas_context.json`：正式海外／亚洲指数及跨市场时点；
- `data/state/account_fact.json`：当日账户事实；
- `data/state/stock_context.json`：动态账户个股层；
- `data/state/stock_market_context.json`：当前账户个股行情；
- `data/state/query_context.json`：ChatGPT按需读取计划；
- `data/state/system_consistency.json`：系统一致性结果。

生产原始响应、快照与审计材料应保持可追溯。失败记录不得伪装成成功快照，历史回放不得写入生产CURRENT。

## 8. 故障与降级

- GitHub Actions延迟：使用真实 `captured_at` 重新判定新鲜度；
- 同花顺单对象／单轮失败：保留上一有效状态并记录FAILED；
- Yahoo单一海外对象失败：该对象局部FAILED，其余A股和海外对象继续；
- 某一正式指数无可用直接源：允许明确代理或FAILED，但不得删除监测职责；
- 数据格式正常但时间戳陈旧：按时点规则降级，不能视为FRESH；
- 当日账户事实缺失：行情采集可以继续，账户、风险率、资金与卖出判断不得引用旧日账户作为当前事实；
- Git推送冲突：数据提交先rebase；不得覆盖正式文件；冲突无法自动解决时保留数据并要求人工检查。

## 9. 维护、实时同步与一致性检查

本规范必须与实际生产系统同步维护，不能成为静态说明文件。

以下任一内容变化，都必须在同一维护批次复核并必要时更新本规范：

- 三层监测结构或监测对象；
- ETF持仓／观察全集的运行清单；
- 数据源、provider优先级、代理关系；
- 脉冲频率、新鲜度阈值、超时、重试、并发、收盘宽限；
- 跨市场时间规则；
- 数据状态文件、查询上下文或workflow；
- 账户事实及动态个股识别机制；
- 新增、停用或替换接口。

一致性检查是基础验收步骤，不是可选专项。任何涉及正式文件、数据规范、监测配置、运行脚本或GitHub Actions的更新，在进入生产前必须执行 `scripts/check_system_consistency.py`。

硬一致性冲突（例如正式ETF名单与运行清单不同、正式指数缺失、规范路径不存在、生产脚本重新硬编码名单、脉冲参数与规范冲突）必须判定FAIL并阻断生产。账户事实缺失、休市等正常状态只记WARNING，不应阻断纯行情采集。

GitHub `main` 是云端唯一主版本；本规范位于一级目录，统一路径为：

`ETF与市场监测数据接口使用规范.md`

## 10. 边界

本规范不修改20万元ETF计划本金、风险许可阈值、Trial／Confirm生命周期、0／5,000／10,000／20,000／30,000元金额档或卖出规则；不自动交易、不自动修改MASTER。

规范的目标是：**确保进入交易系统的行情事实完整、及时、可追溯、跨市场时点正确，并在接口、调度或账户事实异常时以可控方式降级，而不是给系统制造虚假的实时确定性。**
